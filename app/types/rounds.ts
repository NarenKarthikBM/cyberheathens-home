export type RoundOf40Battle = {
  contestantIDs: Array<string>; // Email IDs
  groupName: string;
  hrLink: string;
  winnerID: string;
};

export type Qualifiers = {
  contestantIDs: Array<string>; // Email IDs
  groupName: string;
  hrLink: string;
  winnerID: string;
};

export type Semis = {
  contestantIDs: Array<string>; // Email IDs
  hrLink: string;
  winnerID: string;
};

export type Finals = {
  contestantIDs: Array<string>; // Email IDs
  hrLink: string;
  winnerID: string;
};
